<div class="container">
<div class="cover mb-4 d-flex align-items-center justify-content-center">
  <div class="text-center">
    <h1>Save movies you can't wait to watch 🍿</h1>
    <h3 class="typed-js" data-controller="typed-js"></h3>
  </div>
</div>
<div class="container py-5">
  <div class="d-flex align-items-center justify-content-between mb-3">
    <h3 class="mb-0">My Watch Lists 🎥✨</h3>
    <%= link_to "Create a watch list", new_list_path, class: "btn btn-primary" %>
  </div>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="co-12 col-sm-6 col-md-4 mb-3">
        <%= link_to list_path(list), class:"text-decoration-none" do %>
          <div class="card-category">
            <%= list.name %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
</div>


<%# <div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>All Watch Lists</h1>
      <ul class="list-group mb-3">
        <% @lists.each do |list| %>
          <%# <li class="list-group-item"> %>
            <%# <%= link_to list.name, list_path(list) %>
          <%# </li> %>
        <%# <% end %>
      <%# </ul> %>
      <%# <%= link_to "Add a new List", new_list_path, class: 'btn btn-primary' %>
    <%# </div> %>
  <%# </div> %>
<%# </div> %>
